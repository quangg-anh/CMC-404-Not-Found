[pytest]
asyncio_mode = auto
testpaths = tests
# Dev tokens for Bearer test-admin-* / test-citizen (also set in tests/conftest.py):
#   ENABLE_DEV_TOKENS=1 APP_ENV=local AUTH_TOKEN_SECRET=test-auth-token-secret-at-least-32-chars pytest -vv
