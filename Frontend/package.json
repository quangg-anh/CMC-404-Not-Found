{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "description": "LexSocial AI — unified web (citizen + admin)",
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "start": "serve -s apps/web/dist -l tcp://0.0.0.0:3000",
    "preview": "npm run preview -w web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "serve": "^14.2.4"
  }
}
