# Admin portal -> BE3 API base URL.
# Defaults to http://localhost:8000 when unset (see src/lib/api.ts).
# Copy to `.env` and change only if the backend runs elsewhere.
VITE_API_URL=http://localhost:8000
